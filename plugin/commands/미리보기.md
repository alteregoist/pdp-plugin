---
description: 생성된 상세페이지 HTML을 브라우저로 미리보기 및 스크린샷 캡처
allowed-tools: Read, Write, Bash, Glob, AskUserQuestion, TodoWrite
argument-hint: [HTML 파일 경로]
---

생성된 상세페이지 HTML을 Playwright 브라우저로 열어 미리보기하고 스크린샷을 캡처한다.

## 실행 순서

### Step 1: HTML 파일 찾기

$ARGUMENTS로 HTML 파일 경로가 전달된 경우 해당 파일을 사용한다.
경로가 없으면 작업 디렉토리에서 최근 생성된 상세페이지 HTML 파일을 자동 탐색한다:
- `*_detail_page.html` 패턴으로 Glob 검색
- 여러 개 발견되면 AskUserQuestion으로 선택 요청

### Step 2: Playwright 환경 확인

Node.js Playwright가 설치되어 있는지 확인하고, 없으면 설치한다:
```bash
node -e "require('playwright')" 2>/dev/null || (npm install playwright && npx playwright install chromium)
```

### Step 3: 미리보기 모드 선택

AskUserQuestion으로 미리보기 방식을 확인한다:

- **전체 페이지 캡처** — 전체 상세페이지를 하나의 긴 이미지로 캡처
- **모듈별 캡처** — 각 모듈(A, B, C-1 등)을 개별 이미지로 캡처
- **특정 모듈만** — 원하는 모듈만 선택하여 캡처
- **전체 + 모듈별** — 전체 페이지와 개별 모듈 모두 캡처

### Step 4: 스크린샷 캡처

Node.js + Playwright 스크립트를 작성하여 실행한다:

```javascript
const { chromium } = require('playwright');
const path = require('path');
const fs = require('fs');

(async () => {
  const htmlPath = process.argv[2];
  const outputDir = process.argv[3] || path.join(path.dirname(htmlPath), 'preview');
  const mode = process.argv[4] || 'both'; // 'full' | 'modules' | 'both'

  fs.mkdirSync(outputDir, { recursive: true });

  const browser = await chromium.launch();
  const page = await browser.newPage({ viewport: { width: 860, height: 800 } });

  // HTML 파일 로드
  await page.goto(`file://${path.resolve(htmlPath)}`);
  await page.waitForLoadState('networkidle');

  if (mode === 'full' || mode === 'both') {
    // 전체 페이지 캡처
    await page.screenshot({
      path: path.join(outputDir, 'preview_full.png'),
      fullPage: true,
      type: 'png'
    });
  }

  if (mode === 'modules' || mode === 'both') {
    // 모듈별 캡처
    const modules = await page.$$('[data-module-type]');
    for (const mod of modules) {
      const moduleType = await mod.getAttribute('data-module-type');
      const moduleOrder = await mod.getAttribute('data-module-order');
      await mod.screenshot({
        path: path.join(outputDir, `preview_module_${moduleOrder}_${moduleType}.png`),
        type: 'png'
      });
    }
  }

  await browser.close();
  console.log(`캡처 완료: ${outputDir}`);
})();
```

viewport 너비는 해당 HTML의 `max-w-[...]` 값을 파싱하여 자동 결정한다.
기본값은 860px (롯데마트몰 규격).

### Step 5: 결과 표시

캡처된 이미지를 사용자에게 보여준다:
- 전체 페이지 이미지 링크 제공
- 모듈별 이미지 목록 및 링크 제공
- HTML 파일의 모듈 구성 요약 함께 표시:
```
미리보기 완료:
📄 전체 페이지: preview_full.png
📦 모듈 구성:
  1. [A] 상단배너
  2. [B] 한눈에 보기 (4개 특장점)
  3. [C-1] 컷샷 (3개 블록)
  4. [C-2] 상품정보
  5. [D] 인증정보
  6. [F] 표시사항
```

### Step 6: 후속 액션 제안

미리보기 후 사용자에게 다음 액션을 제안한다:
- 수정이 필요하면 `/수정하기` 커맨드 안내
- JPEG 포맷으로 재캡처 (용량 절감)
- 다른 viewport 너비로 재캡처 (모바일 미리보기 등)
- 특정 모듈만 확대 캡처

### 주의사항

- HTML 내의 이미지 경로(`./images/`)가 올바르게 연결되어 있는지 확인
- 이미지가 없으면 깨진 이미지 영역이 표시됨을 사용자에게 안내
- 캡처 이미지는 HTML 파일과 같은 디렉토리의 `preview/` 폴더에 저장
- TailwindCSS CDN 로드를 위해 네트워크 접근이 필요할 수 있음 (오프라인 시 스타일 미적용 가능)
